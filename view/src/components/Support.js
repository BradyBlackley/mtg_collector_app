const supportStyle = {
    textAlign: "center",
    marginTop: "5px"
}

function Support() {
    return(
        <div className="support" style={supportStyle}>This is the support page</div>
    )
}

export default Support;